/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  compiler: {
    styledComponents: true
  },
  // images: {
  //   path: "/images",
  // },
}

module.exports = nextConfig
